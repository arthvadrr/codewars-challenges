42
function eatFood() {
    let food = 'cheese';
    let time = 1;

    return time + food;
}

const doThing = ( param1 ) => {if (
        thing ||
        thing2
    ) return 'hi'

    return param1.map(() => doThing3() ?? null)
}

hihi